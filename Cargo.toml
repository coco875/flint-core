[package]
name = "flint-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0.100"
# rsjsonnet = "0.4.0" # for jsonnet support

[dev-dependencies]
tempfile = "3.0"